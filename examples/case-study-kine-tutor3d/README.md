# Case Study: KineTutor3D

This folder represents one concrete Unity project that used `unity-cli` custom tools for validation, robot kinematics, session QA, and camera capture.

This is intentionally optional reference material, not the default workflow for the toolkit.
