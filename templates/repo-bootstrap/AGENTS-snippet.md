## Unity CLI Usage
- Read the project CLI guide before using custom tools.
- In PowerShell, prefer `$PSNativeCommandArgumentPassing = "Standard"`.
- Use registered `snake_case` custom tool names.
- Prefer `--params '{"key":"value"}'` for strings, booleans, and comma-separated values.
