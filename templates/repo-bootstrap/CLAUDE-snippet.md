## Unity CLI Rule
- Prefer `unity-cli` over MCP when a project-specific CLI tool already exists.
- In PowerShell, set `$PSNativeCommandArgumentPassing = "Standard"` first.
- Use registered custom tool names like `compile_check_tool`.
- Prefer `--params '{"key":"value"}'` for complex arguments.
